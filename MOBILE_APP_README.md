# تطبيق Voice AI Studio للجوال

توجد نسخة Flutter الأصلية داخل `mobile_app/` بصورة مستقلة تمامًا عن واجهة الكمبيوتر. لا تعتمد النسخة على WebView ولا تغيّر تشغيل نسخة سطح المكتب. التطبيق عربي RTL، يعمل على Android 10 فأحدث، وبنيته قابلة لإضافة iOS لاحقًا من المصدر نفسه.

## ما الذي يعمل على الهاتف؟

- تسجيل WAV من الميكروفون مع الإيقاف المؤقت والاستكمال والإنهاء والحذف والمعاينة.
- اختيار الملفات عبر مدير ملفات النظام وScoped Storage دون صلاحية تخزين عامة.
- تحليل الصوت وتحويل الصيغ محليًا عبر FFmpeg Kit.
- عرض المدة والضوضاء والصمت والتشويه ومعدل العينة ودرجة الجودة.
- اكتشاف خادم الكمبيوتر عبر mDNS أو إدخال عنوانه يدويًا، والاقتران برمز QR مؤقت.
- توليد الصوت، استنساخ الصوت Pro، تجهيز المحركات، تتبع تنزيل النماذج، إلغاء المهام واستئناف متابعتها بعد عودة الشبكة.
- إنشاء عدة مرشحين وتشغيل النتيجة وتنزيلها وحفظها ومشاركتها.
- قراءة PDF وDOCX وTXT، مع تحويل الأرقام والتواريخ والعملات إلى نطق عربي.
- استوديو الشيلات والأغاني وإضافة مسار صوتي اختياري.
- إدارة ملفات الخادم ومشاريع محلية محفوظة.
- إعداد Gemini وElevenLabs وحفظ المفاتيح داخل Android Keystore.
- الوضع الفاتح والداكن وتصميم متجاوب للهواتف والأجهزة اللوحية.

## وضعا التشغيل

### الوضع المحلي

يعمل التسجيل والتشغيل والتحليل الأولي وتحويل الملفات على الهاتف. لا يحاول التطبيق تحميل أو تشغيل XTTS أو نماذج GPU الضخمة على الهاتف، لأن ذلك غير مناسب للذاكرة والحرارة والبطارية.

### وضع الخادم

يعمل FastAPI على الكمبيوتر أو خادم سحابي، ويتولى XTTS والمحركات الثقيلة وقراءة المستندات وتوليد المشاريع الطويلة. الهاتف يبقى واجهة أصلية احترافية كاملة، وليس مجرد صفحة ويب.

## البنية

```text
mobile_app/lib/
├── app/          # التطبيق والتوجيه
├── core/         # المظهر والأخطاء والثوابت والحالة المشتركة
├── features/     # كل ميزة في مجلد مستقل
├── l10n/         # الرسائل العربية
├── models/       # نماذج الجلسة والملفات والمهام والتحليل
├── services/     # API والتسجيل والتشغيل وFFmpeg والأمان والاكتشاف
└── widgets/      # مكونات RTL المتجاوبة المشتركة
```

طبقة الخادم الجديدة داخل `backend/mobile/`، وجميع مسارات سطح المكتب القديمة باقية كما هي.

## أوامر سريعة

Windows، إعداد وفحص واختبار وبناء APK وAAB بأمر واحد:

```bat
BUILD_MOBILE_RELEASE.bat
```

Linux:

```bash
./setup_mobile_app.sh
./build_android_apk.sh
./build_android_aab.sh
```

تشغيل الخادم:

```bash
./run_mobile_backend.sh --advertise-host 192.168.1.5
```

ثم افتح على الكمبيوتر `http://127.0.0.1:8000/mobile-pairing`.

## أدلة إضافية

- [تثبيت Android](MOBILE_ANDROID_INSTALL.md)
- [الاتصال بالخادم](MOBILE_SERVER_CONNECTION.md)
- [دليل الاقتران](MOBILE_PAIRING_GUIDE.md)
- [الأمان والخصوصية](MOBILE_SECURITY.md)
- [حل المشكلات](MOBILE_TROUBLESHOOTING.md)

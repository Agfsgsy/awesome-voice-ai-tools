# سجل التغييرات — استوديو ابن الواقدي

## [6.0.0] - 2026-07-27

### أُضيف

- واجهة **Ultimate Voice** ثنائية اللغة تتحول كاملة بين العربية والإنجليزية.
- كتالوج مركزي صارم للغة والجنس واللهجة، مع رفض أي عدم تطابق بدل التبديل الصامت.
- دعم مباشر لـAzure AI Speech وGoogle Cloud TTS وOpenAI TTS، مع ربط واختبار المفاتيح محليًا.
- تصنيف صريح للمحركات: مجاني، مفتوح المصدر، تجربة/حصة مجانية، أو مدفوع.
- كاتب إبداعي للتأليف وإعادة الصياغة وبناء المقابلات بالعربية والإنجليزية.
- إنتاج مقابلات يوزع صوتًا مطابقًا لكل دور ويعيد خريطة الدور والصوت في النتيجة.

### أُصلح

- تصحيح جنس أصوات Gemini/Google: من ذلك Kore وGacrux كصوتين أنثويين، وCharon وAlgieba كصوتين ذكوريين.
- منع استخدام Piper لصوت امرأة لعدم وجود صوت أنثوي عربي موثق في الحزمة المضمنة.
- منع اختيار صوت OpenAI كرجل أو امرأة لأن المزود لا ينشر تصنيف جنس رسميًا ثابتًا.

### حُسّن

- إضافة اللهجات العربية المتاحة في Edge/Azure وأصوات إنجليزية أمريكية وبريطانية وأسترالية.
- توحيد الإصدار في التطبيق والمثبت والمدقق والواجهات إلى 6.0.0.
- تحديث المحدث ذي الأمر الواحد ليجرب فرع الإصدار الجديد ثم المصدر المستقر.

## [5.1.0] - 2026-07-27

### أُصلح

- إصلاح خطأ `Field required` للموقع `query.req` عند إنتاج الصوت والمقابلات.
- تثبيت طلبات `/api/tts` و`/api/speech` كأجسام JSON موثقة في OpenAPI.
- إضافة فحص عند بدء التطبيق يمنع المسارات المكررة أو تحوّل جسم الطلب إلى query.
- إزالة النغمة الوهمية التي كانت تُعرض كبديل للكلام عند فشل محرك الصوت.
- توحيد التنقل من لوحة التحكم إلى صفحات الإنتاج داخل غلاف الاستوديو.
- منع ضياع المقابلة أو ظهور خطأ 500 عندما يمنع Windows الكتابة على سطح المكتب أو يكون الملف مفتوحًا.

### أُضيف

- سياسة **Free First**: Edge TTS المجاني أولًا وPiper المحلي بديلًا تلقائيًا.
- تنزيل ذري تلقائي لنموذج Piper العربي وحفظه للاستخدام دون اتصال.
- تخزين مؤقت لمخرجات Piper مع دعم السرعة عبر واجهته الحديثة.
- اختبارات ارتداد لعقود JSON، وسياسة المحركات المجانية، والإصدار الموحّد.
- بناء Windows موحّد ينتج نسخة محمولة وملف تثبيت.

### حُسّن

- توحيد اسم المنتج والإصدار إلى «استوديو ابن الواقدي 5.1».
- توضيح المحركات المجانية والسحابية في جميع واجهات الإنتاج.
- جعل Gemini وElevenLabs اختيارات صريحة اختيارية بلا تحويل سحابي تلقائي.
- تحديث ملفات التثبيت والحزم ودعم Python حتى 3.13.

## [2.0.0] - 2026-07-10

### المضاف
- بنية احترافية كاملة (backend/ frontend/ plugins/)
- نظام إضافات Plugin System مع اكتشاف تلقائي
- API كاملة مع 18+ endpoint
- واجهة ويب عربية RTL مع 7 صفحات
- دعم Termux (Android)
- 4 ملفات متطلبات منفصلة (core, termux, linux, dev)
- 11 سكربت تشغيل وصيانة
- أداة فحص ذاتي (Doctor)
- Docker و docker-compose
- دعم Hugging Face Spaces
- محرك TTS موحد مع 8 محركات
- محرك احتياطي كان ينتج نغمة تجريبية
- دعم Google Gemini TTS API
- نظام تسجيل أحداث متكامل
- معالجة استثناءات شاملة
- Async/Await للأداء

### المحسّن
- إعادة هيكلة كاملة للكود
- إصلاح جميع المسارات (Paths)
- إزالة الكود المكرر
- تنظيم Imports
- دعم Dark Mode في الواجهة

### القيود
- محركات TTS الثقيلة (XTTS, F5, Bark) تحتاج تثبيتاً يدوياً
- بعض المكتبات لا تعمل على Termux (torch, torchaudio)
- المحرك الاحتياطي يولد نغمة بسيطة فقط

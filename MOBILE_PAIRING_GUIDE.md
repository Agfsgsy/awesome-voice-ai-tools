# دليل اقتران الهاتف بالكمبيوتر

## الاقتران بواسطة QR

1. شغّل خادم الجوال.
2. افتح على الكمبيوتر فقط:

```text
http://127.0.0.1:8000/mobile-pairing
```

3. أدخل عنوان LAN الذي يستطيع الهاتف الوصول إليه، مثل `http://192.168.1.5:8000`.
4. اضغط «إنشاء جلسة اقتران مؤقتة».
5. في التطبيق اختر «مسح رمز QR» وامنح إذن الكاميرا.
6. بعد النجاح يصدر الخادم سر جهاز طويلًا مرة واحدة، ثم يصدر Access Token قصير العمر تلقائيًا.

رمز QR يحتوي عنوان الخادم ومعرّف جلسة عشوائيًا ورمزًا مؤقتًا. الجلسة أحادية الاستخدام وتنتهي سريعًا، ولا يحتوي التطبيق أي IP أو مفتاح ثابت.

## الاقتران اليدوي

يمكن كتابة عنوان الخادم ومعرّف الجلسة والرمز الظاهر أسفل QR في شاشة الإعداد الأولي.

## إنشاء QR من الطرفية

```bash
.venv/bin/python scripts/generate_mobile_pairing_qr.py http://192.168.1.5:8000
```

يُحفظ PNG افتراضيًا داخل `.mobile_data/` المستثنى من Git.

## عرض الأجهزة وإلغاء الاقتران

من صفحة الاقتران اضغط «إلغاء الاقتران» بجانب الهاتف، أو استخدم:

```bash
.venv/bin/python scripts/generate_mobile_pairing_qr.py --list-devices
.venv/bin/python scripts/generate_mobile_pairing_qr.py --revoke DEVICE_ID
```

بعد الإلغاء يرفض الخادم كل Access Token لذلك الجهاز. لإعادته، أنشئ جلسة QR جديدة.

## انتهاء الجلسة

إذا ظهرت «انتهت صلاحية جلسة الاقتران»، أنشئ QR جديدًا. لا يمكن إعادة استخدام الرمز القديم، وهذه حماية مقصودة.

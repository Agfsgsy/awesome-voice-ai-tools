name: Build Windows Installer

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - "backend/**"
      - "frontend/**"
      - "scripts/**"
      - "desktop_app.py"
      - "main.py"
      - "requirements*.txt"
      - "VoiceAIStudio.spec"
      - "installer/**"
      - ".github/workflows/build-windows-installer.yml"
  push:
    branches:
      - "agent/professional-tts-engine"
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install Python dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip wheel
          python -m pip install -r requirements.txt -r requirements-desktop.txt

      - name: Compile Python source
        shell: pwsh
        run: python -m compileall -q main.py desktop_app.py backend scripts

      - name: Validate unified stable release
        shell: pwsh
        run: python scripts/validate_unified_release.py

      - name: Build desktop application
        shell: pwsh
        run: |
          python -m PyInstaller --noconfirm --clean VoiceAIStudio.spec
          if (-not (Test-Path "dist/VoiceAIStudioArabic/VoiceAIStudioArabic.exe")) {
            throw "Desktop executable was not created."
          }

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --yes --no-progress

      - name: Build installer
        shell: pwsh
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installer\VoiceAIStudio.iss"
          if (-not (Test-Path "dist-installer/VoiceAIStudioSetup.exe")) {
            throw "Installer was not created."
          }

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: IbnWaqadiStudio-5.0-Windows-Setup
          path: dist-installer/VoiceAIStudioSetup.exe
          if-no-files-found: error
          retention-days: 30

      - name: Upload portable folder
        uses: actions/upload-artifact@v4
        with:
          name: IbnWaqadiStudio-5.0-Windows-Portable
          path: dist/VoiceAIStudioArabic
          if-no-files-found: error
          retention-days: 14

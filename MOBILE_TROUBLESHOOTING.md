# حل مشكلات تطبيق الجوال

## التطبيق يطلب عنوان خادم أو QR

في الإصدار المحلي المستقل اضغط «ابدأ الآن من الهاتف»، أو حدّث التطبيق إلى أحدث APK؛ بعدها يفتح تلقائيًا في لوحة التحكم. الاقتران اختياري فقط لـ XTTS والمحركات الثقيلة.

## الصوت العربي غير مثبت

افتح «الإعدادات > محرك الصوت العربي على الهاتف»، ثم اضغط «تنزيل الصوت العربي». ثبّت العربية داخل محرك تحويل النص إلى كلام في Android، ثم ارجع واضغط «فحص المحرك». يختلف اسم الحزمة حسب الشركة المصنّعة، ولا يضع التطبيق نموذجًا أو مفتاحًا سريًا داخل APK.

## الخادم غير متصل

- تأكد أن الهاتف والكمبيوتر على الشبكة نفسها.
- افتح `/api/mobile/status` من متصفح الهاتف.
- استخدم عنوان LAN للكمبيوتر، وليس `127.0.0.1` على الهاتف.
- راجع جدار الحماية والمنفذ.
- للخادم الخارجي استخدم HTTPS وشهادة صحيحة.

## انتهت صلاحية جلسة الاقتران

أنشئ QR جديدًا من `/mobile-pairing`. الرمز أحادي الاستخدام ولا يمكن استعادته.

## التسجيل لا يحتوي كلامًا واضحًا

سجّل ثانيتين على الأقل في مكان أهدأ، قرب الميكروفون دون ملامسته، وتجنب الموسيقى والصدى والقص الصوتي.

## صيغة الملف غير قابلة للفك

جرّب زر «تحويل محلي إلى WAV». إذا فشل، قد يكون الملف تالفًا أو لا يحتوي مسارًا صوتيًا. يدعم التطبيق الصيغ الشائعة وأي صيغة يفكها إصدار FFmpeg المضمن.

## المحرك غير جاهز / النموذج قيد التنزيل

افتح «المحركات والنماذج»، واضغط «تجهيز». تابع الإشعار أو لوحة المهام، ولا تغلق خادم الكمبيوتر أثناء التنزيل. يمكن إلغاء المهمة من زر الإلغاء.

## فشل رفع الملف أو انقطع الإنترنت

اترك المهمة ظاهرة؛ يستأنف التطبيق الرفع من آخر جزء محفوظ. تأكد من وجود مساحة على الهاتف والخادم، ثم اضغط تحديث حالة الاتصال.

## المساحة غير كافية

احذف ملفات قديمة من «المشاريع والملفات»، وأفرغ مساحة على الكمبيوتر من `outputs/` و`.mobile_data/uploads/` بعد التأكد من عدم الحاجة إليها.

## الملف الناتج غير صالح

تحقق من جاهزية المحرك ومن سجل الخادم، وجرب نصًا أقصر أو محركًا آخر. لا يتيح التطبيق تشغيل أو مشاركة ملف حجمه صفر.

## لا تعمل الكاميرا أو الميكروفون

افتح إعدادات Android > التطبيقات > Voice AI Studio > الأذونات، واسمح بالصلاحية المطلوبة فقط. يمكن إدخال رمز الاقتران يدويًا دون الكاميرا.

## فشل بناء Android

نفّذ:

```bash
flutter doctor -v
cd mobile_app
flutter clean
flutter pub get
flutter analyze --fatal-infos
flutter test
```

تأكد من JDK 17 وAndroid SDK ومن قبول التراخيص. سكربت البناء ينشئ Gradle wrapper من Flutter المثبت إذا لم يكن موجودًا.

## لا يمكن تثبيت Release

ملف CI المسمى `unsigned` يحتاج توقيعًا. للتجربة ابنِ Debug:

```bash
python scripts/build_android_app.py --apk --debug
adb install -r dist/mobile/android/voice-ai-mobile-debug.apk
```

# اتصال الهاتف بخادم Voice AI

## تشغيل الخادم داخل الشبكة المحلية

ثبّت المتطلبات مرة واحدة:

```bash
python scripts/setup_mobile_backend.py
```

اعرف عنوان الكمبيوتر داخل الشبكة، مثل `192.168.1.5`، ثم شغّل:

```bash
./run_mobile_backend.sh --advertise-host 192.168.1.5 --port 8000
```

على Windows:

```bat
RUN_MOBILE_BACKEND.bat --advertise-host 192.168.1.5 --port 8000
```

تأكد أن الهاتف والكمبيوتر على شبكة Wi-Fi نفسها، واسمح للمنفذ 8000 داخل ملف تعريف الشبكة الخاصة في جدار الحماية. لا تفتح المنفذ للإنترنت مباشرة.

يمكن للتطبيق البحث تلقائيًا عن خدمة `_voiceai._tcp.local`، أو إدخال `http://192.168.1.5:8000` يدويًا. لا يوجد عنوان IP ثابت داخل التطبيق.

## اختبار الحالة

من متصفح الهاتف افتح:

```text
http://192.168.1.5:8000/api/mobile/status
```

ظهور JSON بحالة `online` يؤكد وصول الشبكة فقط؛ العمليات الحساسة تبقى محمية برمز الجهاز.

## التشغيل خارج الشبكة

استخدم اسم نطاق وشهادة TLS صحيحة ومُحدّثة، وضع FastAPI خلف reverse proxy موثوق مثل Caddy أو Nginx. مثال تشغيل TLS مباشر للاختبار الإداري:

```bash
./run_mobile_backend.sh \
  --host 0.0.0.0 \
  --port 8443 \
  --advertise-host voice.example.com \
  --ssl-certfile /secure/path/fullchain.pem \
  --ssl-keyfile /secure/path/privkey.pem
```

طبقة الجوال ترفض HTTP للعميل الخارجي. لا تستخدم port forwarding مباشرًا دون TLS ومصادقة، ويفضل VPN مثل WireGuard أو Tailscale مع جدار حماية.

عند استخدام reverse proxy، ضبط `MOBILE_TRUST_PROXY_HEADERS=true` فقط إذا كان FastAPI لا يستقبل الاتصال إلا من الوكيل الموثوق. مرّر `X-Forwarded-Proto` بصورة صحيحة، ولا تنشر مسار `/mobile-pairing` ولا `/api/mobile/admin/*` للإنترنت.

## انقطاع الشبكة

- يرفع التطبيق الملفات على أجزاء 5 MiB، ويسأل الخادم عن آخر إزاحة بعد عودة الاتصال.
- تحفظ هويات المهام الطويلة في المخزن الآمن ويُستأنف الاستعلام عن تقدمها تلقائيًا.
- يمكن إلغاء المهمة من شاشة التقدم.
- التنزيل يستخدم ملف `.part` وطلب HTTP Range للاستكمال.

## حدود الهاتف

FFmpeg والتحليل الخفيف والتسجيل والتشغيل محلية. XTTS وتنزيل النماذج وتوليد عدة مرشحين وقراءة مستندات كبيرة تعمل على الكمبيوتر أو الخادم. هذا الفصل يمنع نفاد الذاكرة وارتفاع حرارة الهاتف.

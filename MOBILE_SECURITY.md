# أمان تطبيق الجوال

## الأسرار والرموز

- لا توجد مفاتيح API أو عناوين خادم ثابتة في المصدر.
- يحفظ `flutter_secure_storage` سر الجهاز وAccess Token ومفاتيح Gemini وElevenLabs داخل Android Keystore.
- لا يعرض Endpoint الأجهزة قيمة hash السرية.
- Access Token موقع، قصير العمر، ومقيد بجهاز غير ملغى.
- رموز مشاركة الملفات موقعة ومؤقتة.
- معرفات الملفات موقعة ولا تسمح بتمرير مسارات نظام الملفات.
- لا يرسل التطبيق مفاتيح مزودي الخدمات إلا عندما يبدأ عنوان الخادم بـ`https://`.

## الاقتران

- جلسة QR قصيرة العمر، عشوائية، أحادية الاستخدام ومحدودة المحاولات.
- صفحة إدارة الأجهزة تتطلب عميلًا واسم مضيف loopback على الكمبيوتر فقط، ويجب عدم نشر `/mobile-pairing` عبر reverse proxy.
- يستطيع المستخدم إلغاء الهاتف فورًا من الصفحة أو CLI.

## استنساخ الصوت

لا يرسل التطبيق طلب استنساخ حتى:

1. يختار المستخدم «أنا صاحب الصوت» أو «لدي إذن صريح».
2. يكتب إقرارًا واضحًا.
3. يحدد مربع الموافقة.
4. ينجح تحليل وجود كلام واضح.

يسجل الخادم وقت العملية ومعرف الجهاز وhash الإقرار والمرجع، ولا يحفظ نص الإقرار في سجل التدقيق.

## صلاحيات Android

| الصلاحية | الاستخدام |
|---|---|
| `RECORD_AUDIO` | التسجيل بعد ضغط المستخدم زر البدء |
| `CAMERA` | مسح QR فقط |
| `POST_NOTIFICATIONS` | تقدم المهام الطويلة على Android 13+ |
| `INTERNET` و`ACCESS_NETWORK_STATE` | الاتصال بالخادم ومراقبة الشبكة |
| `CHANGE_WIFI_MULTICAST_STATE` | اكتشاف mDNS داخل LAN |

لا يطلب التطبيق `MANAGE_EXTERNAL_STORAGE` ولا صلاحيات قراءة كل الصور أو الوسائط. File Picker وSave Picker يستخدمان Storage Access Framework وScoped Storage.

حزمة FFmpeg المستخدمة هي النسخة الصوتية غير GPL (`ffmpeg_kit_flutter_new_audio`) لتوفير MP3 وOpus وVorbis وAMR مع تجنب إدخال مرمزات فيديو GPL غير اللازمة. يجب إبقاء إشعارات LGPL الخاصة بـFFmpeg ضمن حزمة التوزيع وفق ترخيص المكتبة.

## HTTP وHTTPS

يسمح Android cleartext تقنيًا لأن عنوان LAN متغير ولا يمكن إدراجه مسبقًا في Network Security Config، لكن التطبيق نفسه يرفض HTTP لأي مضيف غير خاص أو loopback أو `.local`. الخادم يرفض أيضًا عميلًا خارجيًا دون HTTPS. للخارج استخدم TLS صحيحًا ويفضل VPN.

## التوقيع وCI

لا يُحفظ keystore في المستودع. يقرأ GitHub Actions التوقيع من GitHub Secrets عند توفره، ويحذف ملفات التوقيع بعد البناء. عند عدم توفر الأسرار، يرفع ملفات غير موقعة للمراجعة ولا يدّعي أنها حزمة نشر نهائية.

## بيانات التشغيل

يخزن الخادم أسراره والأجهزة والمهام والرفع المؤقت داخل `.mobile_data/` بصلاحيات ملف مقيدة حيث يدعم النظام ذلك، والمجلد مستثنى من Git. احمه ضمن نسخك الاحتياطية ولا تشاركه.
